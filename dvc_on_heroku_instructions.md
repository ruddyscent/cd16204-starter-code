# Local project workflow

The project uses local data, saved inference artifacts, and Uvicorn in the
Workspace. Heroku, DVC, buildpacks, cloud storage, and startup downloads are not
required. Follow [the README](README.md) for setup, validation, real HTTP
evidence, and the self-contained submission ZIP.
