# Validated with Python 3.13.15 in Workspace and 3.12.3 locally on Linux.
# See PYTHON_3_13_UPDATES.md for verification scope.
# Preserve existing compatible pins; transitive dependencies are resolved by pip.

# API framework, validation, and HTTP server
fastapi==0.117.1
pydantic==2.11.9
uvicorn==0.36.0

# Training, tabular preprocessing, and evaluation
numpy==2.3.3
pandas==2.3.2
scikit-learn==1.7.2

# API tests, real HTTP requests, and local validation
httpx==0.28.1
requests==2.33.0
pytest==9.1.1
flake8==7.3.0
